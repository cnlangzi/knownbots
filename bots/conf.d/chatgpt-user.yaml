# OpenAI ChatGPT user agent
# https://developers.openai.com/api/docs/bots
kind: AIAssist
name: chatgpt-user
# OpenAI uses the same JSON format as Google ({"prefixes": [{"ipv4Prefix": ...}]})
parser: google
ua: "ChatGPT-User"
urls:
  - "https://openai.com/chatgpt-user.json"
