# Meta AI/Facebook crawler
# Uses RDNS verification as Meta doesn't publish public IP list
# https://developers.cloudflare.com/bots/concepts/bot/verified-bots/
kind: AITraining
name: meta-externalagent
ua: "Meta-ExternalAgent"
rdns: true
domains:
  - "fbsv.net"
